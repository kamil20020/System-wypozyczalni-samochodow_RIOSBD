version: '3'
include:
  - ./database/docker-compose.yaml
services:
  backend:
    image: kamil20/backend
    build:
      context: ./backend
    container_name: backend
    restart: always
    environment:
      ID: 11111
    ports:
      - "9000:8080"
    depends_on:
      - nginx